import { playlistGenerationStatus } from 'local-listen/helpers/playlist-generation-status';
import { module, test } from 'qunit';

module('Unit | Helper | playlist generation status');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = playlistGenerationStatus([42]);
  assert.ok(result);
});
